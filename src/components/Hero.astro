---
const socials = [
  { href: "https://pe.linkedin.com/in/lrda92", icon: "LinkedIn", label: "LinkedIn" },
  { href: "https://github.com/alejandro-rda", icon: "GitHub", label: "GitHub" },
  { href: "https://alejandror-da.medium.com/", icon: "Medium", label: "Medium" },
  { href: "mailto:alejandror.devops@gmail.com", icon: "Email", label: "Email" },
  { href: "https://open.spotify.com/episode/0czJwNqovlHKCkANsFhES2", icon: "Podcast", label: "Podcast" },
];
---
<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden pt-16">
  <!-- Animated grid background -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute inset-0" style="background-image: linear-gradient(rgba(0,212,255,0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(0,212,255,0.05) 1px, transparent 1px); background-size: 60px 60px;"></div>
  </div>

  <!-- Floating orbs parallax -->
  <div class="parallax-bg absolute top-20 left-10 w-72 h-72 bg-accent/5 rounded-full blur-3xl"></div>
  <div class="parallax-bg absolute bottom-20 right-10 w-96 h-96 bg-accent-alt/5 rounded-full blur-3xl"></div>

  <div class="relative z-10 text-center px-6 max-w-5xl mx-auto">
    <!-- Location badge -->
    <div class="reveal inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-border bg-surface/50 mb-8">
      <span class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></span>
      <span class="text-xs font-mono text-text-muted">Madrid, Spain · Open to hybrid/remote</span>
    </div>

    <!-- Name -->
    <h1 id="hero-name" class="text-5xl sm:text-7xl md:text-8xl lg:text-9xl font-black tracking-tighter mb-4 leading-none">
      <span class="block text-text">ALEJANDRO</span>
      <span class="block gradient-text">RUIZ DEL ÁGUILA</span>
    </h1>

    <!-- Rotating subtitle -->
    <div class="h-10 mb-8 overflow-hidden">
      <div id="subtitle-rotator" class="font-mono text-lg sm:text-xl text-accent">
        <span class="subtitle-item block">DevOps &amp; Cloud Architect</span>
        <span class="subtitle-item block">Football Data Scientist</span>
        <span class="subtitle-item block">Platform Engineer</span>
        <span class="subtitle-item block">GenAI Ops Innovator</span>
      </div>
    </div>

    <!-- Tagline -->
    <p class="reveal text-base sm:text-lg text-text-muted max-w-2xl mx-auto mb-10 leading-relaxed">
      8+ years building multi-cloud platforms &amp; data-intensive systems.<br class="hidden sm:block" />
      From automating CI/CD pipelines to analyzing football set pieces with Poisson models — I bridge <span class="text-accent">technology</span> and <span class="text-accent-alt">sport</span>.
    </p>

    <!-- Social links -->
    <div class="reveal flex flex-wrap items-center justify-center gap-4">
      {socials.map((s) => (
        <a href={s.href} target="_blank" rel="noopener noreferrer"
           class="group flex items-center gap-2 px-4 py-2 rounded-lg border border-border bg-surface/50 hover:border-accent hover:bg-surface-light transition-all duration-300 text-sm font-mono text-text-muted hover:text-accent">
          <span>{s.icon}</span>
        </a>
      ))}
    </div>

    <!-- Scroll indicator -->
    <div class="reveal absolute bottom-10 left-1/2 -translate-x-1/2">
      <div class="w-5 h-8 rounded-full border-2 border-text-muted/30 flex justify-center pt-1">
        <div class="w-1 h-2 rounded-full bg-accent animate-bounce"></div>
      </div>
    </div>
  </div>
</section>
